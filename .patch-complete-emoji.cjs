// Fix: Complete emoji and wide character support based on Unicode standards
const fs = require('fs');
let s = fs.readFileSync('src/term.js', 'utf8');

const oldCharWidth = "export function charWidth(ch) {\n  const c = ch.codePointAt(0);\n  // Tab is special: renders as 4 spaces in most terminals\n  if (c === 0x09) return 4;\n  // zero-width / combining marks\n  if (c < 0x300) return c >= 0x20 ? 1 : 0; // control/space like";

const newCharWidth = "export function charWidth(ch) {\n  const c = ch.codePointAt(0);\n  // Tab is special: renders as 4 spaces in most terminals\n  if (c === 0x09) return 4;\n  // Zero-width characters (combining marks, variation selectors, etc.)\n  if ((c >= 0x0300 && c <= 0x036F) || // Combining diacritical marks\n      (c >= 0x1AB0 && c <= 0x1AFF) || // Combining diacritical marks extended\n      (c >= 0x1DC0 && c <= 0x1DFF) || // Combining diacritical marks supplement\n      (c >= 0x20D0 && c <= 0x20FF) || // Combining marks extension\n      (c >= 0xFE00 && c <= 0xFE0F) || // Variation selectors\n      (c >= 0xE0100 && c <= 0xE01EF)) return 0; // Variation selectors supplement\n  // Control characters and space\n  if (c < 0x20) return 0;\n  if (c === 0x20) return 1; // Space\n  // CJK / wide blocks -> 2\n  if (c >= 0x1100 && c <= 0x115F) return 2; // Hangul Jamo\n  if (c >= 0x2E80 && c <= 0x303E) return 2; // CJK Radicals..CJK Symbols and Punctuation\n  if (c >= 0x3040 && c <= 0xA4CF) return 2; // Hiragana..Yi Syllables\n  if (c >= 0xAC00 && c <= 0xD7A3) return 2; // Hangul Syllables\n  if (c >= 0xF900 && c <= 0xFAFF) return 2; // CJK Compatibility Ideographs\n  if (c >= 0xFE10 && c <= 0xFE19) return 2; // Vertical forms\n  if (c >= 0xFE30 && c <= 0xFE6F) return 2; // CJK Compatibility Forms\n  if (c >= 0xFF00 && c <= 0xFF60) return 2; // Fullwidth ASCII Variants\n  if (c >= 0xFFE0 && c <= 0xFFE6) return 2; // Fullwidth Symbol Variants\n  if (c >= 0x20000 && c <= 0x2FFFD) return 2; // CJK Extension B\n  if (c >= 0x30000 && c <= 0x3FFFD) return 2; // CJK Extension C+D+E+F\n  // Emoji / pictographs render as 2 cells in virtually every terminal.\n  // Comprehensive emoji ranges based on Unicode Standard Annex #51\n  if (c >= 0x00A0 && c <= 0x00AF) return 2; // Latin-1 Supplement (some symbols)\n  if (c >= 0x00B0 && c <= 0x00BF) return 2; // Latin-1 Supplement (symbols)\n  if (c >= 0x0300 && c <= 0x036F) return 0; // Combining marks (already handled above)\n  if (c >= 0x1F000 && c <= 0x1F2FF) return 2; // Mahjong, Dominoes, Playing Cards\n  if (c >= 0x1F300 && c <= 0x1F9FF) return 2; // Misc Symbols and Pictographs, Emoticons, Transport and Map Symbols, Supplemental Symbols and Pictographs, Emoticons (extended), Chess, Symbols and Pictographs Extended-A\n  if (c >= 0x1FA00 && c <= 0x1FA6F) return 2; // Chess Symbols\n  if (c >= 0x1FA70 && c <= 0x1FADFF) return 2; // Symbols and Pictographs Extended-A\n  if (c >= 0x2600 && c <= 0x26FF) return 2; // Misc Symbols\n  if (c >= 0x2700 && c <= 0x27BF) return 2; // Dingbats\n  if (c >= 0x2900 && c <= 0x297F) return 2; // Bracketed Square Elements\n  if (c >= 0x2B00 && c <= 0x2BFF) return 2; // Misc Symbols and Pictographs Extended-B\n  if (c >= 0x1F1E6 && c <= 0x1F1FF) return 2; // Regional Indicator Symbols (flags)\n  // Specific glyphs whose width differs from the naive east-asian table.\n  // 0x276F ❯ is used as the user-message prompt icon. It renders as ONE\n  // column in Windows Terminal / most terminals; counting it as 2 here made\n  // fitAnsi under-pad by one column, shoving the scrollbar glyph one cell left\n  // of the right edge (misaligned scrollbar on user lines).\n  if (c === 0x276F) return 1;\n  if (c === 0x2691 || c === 0x2692) return 2; // ⚑ ⚒\n  if (c === 0x25C6) return 2; // ◆ diamond — rendered wide in many fonts\n  if (c === 0x25CF) return 1; // ● narrow\n  if (c === 0x21B7) return 1; // ↳\n  if (c === 0x25B8 || c === 0x25A1) return 1; // ▸ □\n  if (c === 0x2192 || c === 0x2190) return 1; // → ←\n  if (c === 0x25B2 || c === 0x25BC) return 1; // ▲ ▼\n  if (c === 0x00B7) return 1; // · middle dot\n  if (c === 0x275A) return 1; // ❚\n  if (c === 0x2728) return 2; // ✨ sparkles\n  if (c === 0x2B50) return 2; // ⭐ star\n  if (c === 0x26BD) return 2; // ⚽ soccer ball\n  if (c === 0x26BE) return 2; // ⚾ baseball\n  if (c === 0x1F5BC) return 2; // 🖼️ framed picture\n  if (c === 0x2764) return 2; // ❤ heart (with FE0F variant)\n  return 1;";

const n = s.split(oldCharWidth).length - 1;
if (n !== 1) throw new Error('expected 1 match for charWidth, found ' + n);
s = s.replace(oldCharWidth, newCharWidth);
fs.writeFileSync('src/term.js', s, 'utf8');
console.log('ok: complete emoji ranges added');
